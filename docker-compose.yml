services:
  expenses_bot:
    build:
      context: .
      dockerfile: Dockerfile.local
    user: "${UID}:${GID}"
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped